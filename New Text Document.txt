<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>we talk · we fix · we stay</title>
<meta name="description" content="a quiet place for us">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300..900;1,9..144,300..900&family=DM+Sans:ital,wght@0,300..700;1,300..700&display=swap" rel="stylesheet">
<style>
* { box-sizing: border-box; margin: 0; padding: 0; }
:root {
  --bg: #f1e6d2;
  --bg-warm: #e9d9bf;
  --paper: #faf3e4;
  --paper-soft: #f5ecd9;
  --ink: #2a1d12;
  --ink-soft: #4d3a2a;
  --ink-faded: #8c7660;
  --rose: #b85c4e;
  --rose-deep: #92402f;
  --rose-soft: #e0a89e;
  --sage: #7a8a6f;
  --sage-deep: #586b4f;
  --gold: #c19056;
  --gold-soft: #e3c08c;
  --shadow: rgba(58, 40, 28, 0.08);
  --shadow-deep: rgba(58, 40, 28, 0.18);
  --shadow-warm: rgba(184, 92, 78, 0.12);
}
html { scroll-behavior: smooth; }
body {
  font-family: 'DM Sans', sans-serif;
  background: var(--bg);
  color: var(--ink);
  line-height: 1.6;
  font-weight: 400;
  overflow-x: hidden;
  -webkit-font-smoothing: antialiased;
}
body::before {
  content: '';
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 100;
  opacity: 0.4;
  background-image: url("data:image/svg+xml;utf8,<svg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='3' stitchTiles='stitch'/><feColorMatrix values='0 0 0 0 0.16 0 0 0 0 0.11 0 0 0 0 0.07 0 0 0 0.07 0'/></filter><rect width='100%25' height='100%25' filter='url(%23n)'/></svg>");
  mix-blend-mode: multiply;
}
body::after {
  content: '';
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 99;
  background: radial-gradient(ellipse at center, transparent 40%, rgba(120, 70, 40, 0.12) 100%);
}
nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 50;
  padding: 22px 5vw;
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: linear-gradient(to bottom, rgba(241, 230, 210, 0.95) 0%, rgba(241, 230, 210, 0.7) 80%, transparent 100%);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
}
.nav-mark {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 14px;
  letter-spacing: 0.08em;
  color: var(--rose);
  font-weight: 400;
}
.nav-links {
  display: flex;
  gap: 28px;
  font-size: 12px;
  letter-spacing: 0.18em;
  text-transform: lowercase;
}
.nav-links a {
  color: var(--ink-soft);
  text-decoration: none;
  position: relative;
  padding: 4px 0;
  transition: color 0.3s ease;
}
.nav-links a::after {
  content: '';
  position: absolute;
  left: 0;
  bottom: 0;
  width: 0;
  height: 1px;
  background: var(--rose);
  transition: width 0.3s ease;
}
.nav-links a:hover { color: var(--rose); }
.nav-links a:hover::after { width: 100%; }
@media (max-width: 720px) {
  .nav-links { display: none; }
  nav { padding: 16px 5vw; justify-content: center; }
}
.hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 120px 6vw 80px;
  position: relative;
}
.hero-decoration {
  position: absolute;
  top: 18%;
  right: 8%;
  width: 200px;
  height: 200px;
  opacity: 0.5;
  pointer-events: none;
}
.hero-decoration-2 {
  position: absolute;
  bottom: 12%;
  left: 6%;
  width: 140px;
  opacity: 0.35;
  pointer-events: none;
}
.hero-inner {
  text-align: center;
  max-width: 920px;
  width: 100%;
  position: relative;
  z-index: 2;
}
.eyebrow {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 16px;
  color: var(--rose);
  letter-spacing: 0.04em;
  margin-bottom: 18px;
  opacity: 0;
  animation: fadeUp 0.9s 0.2s ease-out forwards;
}
.eyebrow::before, .eyebrow::after {
  content: '·';
  margin: 0 14px;
  color: var(--ink-faded);
}
.promise {
  font-family: 'Fraunces', serif;
  font-weight: 300;
  color: var(--ink);
  margin-bottom: 60px;
  line-height: 0.95;
}
.promise span {
  display: block;
  font-size: clamp(48px, 11vw, 144px);
  letter-spacing: -0.04em;
  opacity: 0;
  animation: fadeUp 1s ease-out forwards;
}
.promise span:nth-child(1) { animation-delay: 0.4s; }
.promise span:nth-child(2) {
  animation-delay: 0.8s;
  font-style: italic;
  color: var(--rose-deep);
  font-weight: 400;
}
.promise span:nth-child(3) { animation-delay: 1.2s; }
.clocks {
  display: flex;
  align-items: stretch;
  justify-content: center;
  gap: 28px;
  margin: 40px auto;
  max-width: 820px;
  opacity: 0;
  animation: fadeUp 1s 1.6s ease-out forwards;
}
.clock {
  flex: 1;
  background: var(--paper);
  border: 1px solid rgba(140, 118, 96, 0.2);
  border-radius: 6px;
  padding: 24px 18px 22px;
  position: relative;
  text-align: center;
  box-shadow: 0 4px 20px var(--shadow);
}
.clock-stamp {
  position: absolute;
  top: 12px;
  right: 14px;
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 11px;
  color: var(--rose);
  letter-spacing: 0.16em;
  text-transform: lowercase;
}
.clock-city {
  font-family: 'DM Sans', sans-serif;
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 0.28em;
  color: var(--ink-faded);
  margin-bottom: 4px;
}
.clock-name {
  font-family: 'Fraunces', serif;
  font-size: 19px;
  font-weight: 400;
  color: var(--ink);
  margin-bottom: 18px;
  font-style: italic;
}
.clock-analog {
  width: 160px;
  height: 160px;
  display: block;
  margin: 0 auto 18px;
}
.clock-face { fill: var(--paper-soft); stroke: var(--ink-soft); stroke-width: 0.8; }
.clock-face-inner { fill: none; stroke: var(--ink-faded); stroke-width: 0.4; opacity: 0.5; }
.tick { stroke: var(--ink-faded); stroke-width: 1; stroke-linecap: round; }
.tick.major { stroke: var(--ink); stroke-width: 2.4; }
.clock-numeral {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 11px;
  fill: var(--ink-soft);
  text-anchor: middle;
  dominant-baseline: central;
}
.hour-hand, .minute-hand, .second-hand { stroke-linecap: round; }
.hour-hand { stroke: var(--ink); stroke-width: 3.4; }
.minute-hand { stroke: var(--ink); stroke-width: 2; }
.second-hand { stroke: var(--rose); stroke-width: 1.1; }
.clock-center { fill: var(--ink); }
.clock-pin { fill: var(--rose); }
.clock-meta {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: clamp(15px, 1.6vw, 18px);
  color: var(--ink-soft);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  min-height: 30px;
  line-height: 1.35;
  font-weight: 500;
  flex-wrap: wrap;
}
.status-dot {
  display: inline-block;
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--sage);
  box-shadow: 0 0 0 3px rgba(122, 138, 111, 0.2);
  animation: pulse 2.4s ease-in-out infinite;
  flex-shrink: 0;
}
.status-dot.asleep {
  background: var(--ink-faded);
  box-shadow: 0 0 0 3px rgba(140, 118, 96, 0.2);
  animation: none;
}
.status-dot.together {
  background: var(--rose);
  box-shadow: 0 0 0 3px rgba(184, 92, 78, 0.25);
}
@keyframes pulse {
  0%, 100% { box-shadow: 0 0 0 3px rgba(122, 138, 111, 0.2); }
  50% { box-shadow: 0 0 0 6px rgba(122, 138, 111, 0.05); }
}
.between {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 13px;
  color: var(--ink-faded);
  padding: 0 4px;
  min-width: 110px;
}
.between-line {
  width: 70px;
  height: 1px;
  background: var(--ink-faded);
  margin: 10px 0;
  position: relative;
}
.between-line::before, .between-line::after {
  content: '';
  position: absolute;
  top: -2px;
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: var(--rose);
}
.between-line::before { left: -2px; }
.between-line::after { right: -2px; }
.between-distance {
  font-size: clamp(15px, 1.8vw, 22px);
  letter-spacing: 0.18em;
  text-transform: uppercase;
  font-style: normal;
  color: var(--rose-deep);
  font-weight: 700;
}
.between-tz {
  font-size: clamp(14px, 1.5vw, 18px);
  font-style: italic;
  color: var(--rose);
  margin-top: 4px;
  font-weight: 600;
}
@media (max-width: 720px) {
  .clocks { flex-direction: column; gap: 18px; }
  .between { padding: 8px 0; min-width: 0; flex-direction: row; gap: 14px; }
  .between-line { width: 50px; margin: 0; }
}
.days-counter {
  margin-top: 36px;
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: clamp(22px, 3vw, 34px);
  color: var(--ink-soft);
  opacity: 0;
  animation: fadeUp 1s 2s ease-out forwards;
  line-height: 1.5;
}
.days-counter strong {
  color: var(--rose);
  font-weight: 700;
  font-style: normal;
  font-size: 1.35em;
}
.days-counter-sub {
  display: block;
  margin-top: 6px;
  font-size: clamp(15px, 1.6vw, 19px);
  color: var(--ink-faded);
  letter-spacing: 0.04em;
}
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}
section { padding: 100px 6vw; position: relative; }
.section-header { max-width: 880px; margin: 0 auto 60px; text-align: center; }
.section-number {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 13px;
  color: var(--rose);
  letter-spacing: 0.3em;
  margin-bottom: 14px;
}
.section-title {
  font-family: 'Fraunces', serif;
  font-weight: 300;
  font-size: clamp(36px, 6vw, 64px);
  letter-spacing: -0.02em;
  line-height: 1.05;
  color: var(--ink);
  margin-bottom: 14px;
}
.section-title em { font-style: italic; color: var(--rose-deep); font-weight: 400; }
.section-tag {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 17px;
  color: var(--ink-soft);
  max-width: 600px;
  margin: 0 auto;
  line-height: 1.5;
}
.pause-section { background: linear-gradient(180deg, var(--bg) 0%, var(--paper-soft) 100%); }
.pause-content { max-width: 540px; margin: 0 auto; text-align: center; }
.breath-stage {
  position: relative;
  width: 280px;
  height: 280px;
  margin: 0 auto 36px;
  display: flex;
  align-items: center;
  justify-content: center;
}
.breath-circle {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  background: radial-gradient(circle at 30% 30%, var(--rose-soft), var(--rose) 70%);
  box-shadow: 0 20px 60px var(--shadow-warm), inset 0 -10px 30px rgba(146, 64, 47, 0.3);
  transform: scale(0.55);
  transition: transform 4s cubic-bezier(0.4, 0, 0.6, 1);
  position: relative;
}
.breath-circle::before {
  content: '';
  position: absolute;
  inset: -20px;
  border-radius: 50%;
  border: 1px solid var(--rose-soft);
  opacity: 0.4;
}
.breath-circle::after {
  content: '';
  position: absolute;
  inset: -40px;
  border-radius: 50%;
  border: 1px solid var(--rose-soft);
  opacity: 0.2;
}
.breath-circle.in { transform: scale(1); transition-duration: 4s; transition-timing-function: ease-in-out; }
.breath-circle.hold { transform: scale(1); transition-duration: 0s; }
.breath-circle.out { transform: scale(0.55); transition-duration: 6s; transition-timing-function: ease-in-out; }
.breath-text {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 22px;
  color: var(--paper);
  letter-spacing: 0.04em;
  text-shadow: 0 1px 4px rgba(0,0,0,0.15);
  pointer-events: none;
  z-index: 2;
  white-space: nowrap;
}
.pause-controls { display: flex; flex-direction: column; align-items: center; gap: 16px; }
.pause-timer {
  font-family: 'Fraunces', serif;
  font-size: 32px;
  font-weight: 300;
  color: var(--ink);
  letter-spacing: 0.02em;
}
.btn {
  font-family: 'DM Sans', sans-serif;
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.24em;
  padding: 14px 32px;
  border: none;
  background: var(--ink);
  color: var(--paper);
  border-radius: 100px;
  cursor: pointer;
  transition: all 0.3s ease;
  font-weight: 500;
}
.btn:hover { background: var(--rose); transform: translateY(-1px); box-shadow: 0 8px 20px var(--shadow-warm); }
.btn-soft {
  background: transparent;
  color: var(--ink);
  border: 1px solid var(--ink-faded);
}
.btn-soft:hover { background: var(--rose); color: var(--paper); border-color: var(--rose); }
.btn-small {
  font-family: 'DM Sans', sans-serif;
  font-size: 11px;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  padding: 9px 18px;
  border: 1px solid var(--ink-faded);
  background: transparent;
  color: var(--ink-soft);
  border-radius: 100px;
  cursor: pointer;
  transition: all 0.3s ease;
  font-weight: 500;
}
.btn-small:hover { background: var(--rose); color: var(--paper); border-color: var(--rose); }
.pause-prompt {
  margin-top: 36px;
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 18px;
  color: var(--ink-soft);
  line-height: 1.5;
}
.pause-prompt::before { content: '"'; color: var(--rose); margin-right: 4px; }
.pause-prompt::after { content: '"'; color: var(--rose); margin-left: 4px; }
.vow-section { background: var(--bg); position: relative; padding-bottom: 140px; }
.vow-list {
  max-width: 860px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0;
}
.vow-item {
  width: 100%;
  text-align: center;
  padding: 60px 4vw;
  opacity: 0;
  transform: translateY(40px);
  transition: opacity 1.4s ease-out, transform 1.4s ease-out;
}
.vow-item.visible { opacity: 1; transform: translateY(0); }
.vow-text {
  font-family: 'Fraunces', serif;
  font-size: clamp(24px, 4vw, 40px);
  font-weight: 300;
  line-height: 1.32;
  color: var(--ink);
  letter-spacing: -0.012em;
  max-width: 760px;
  margin: 0 auto;
}
.vow-text em { font-style: italic; color: var(--rose-deep); font-weight: 400; }
.vow-text strong { font-weight: 600; color: var(--ink); font-style: normal; }
.vow-divider {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 8px 0;
  opacity: 0;
  transition: opacity 1s ease-out;
}
.vow-divider.visible { opacity: 1; }
.vow-divider svg { width: 80px; height: 14px; display: block; }
.vow-closing {
  text-align: center;
  margin: 80px auto 0;
  padding: 60px 30px;
  max-width: 760px;
  border-top: 1px solid rgba(140, 118, 96, 0.25);
  opacity: 0;
  transform: translateY(30px);
  transition: opacity 1.4s ease-out, transform 1.4s ease-out;
}
.vow-closing.visible { opacity: 1; transform: translateY(0); }
.vow-closing-mark {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 13px;
  color: var(--rose);
  letter-spacing: 0.32em;
  text-transform: uppercase;
  margin-bottom: 26px;
}
.vow-closing-text {
  font-family: 'Fraunces', serif;
  font-size: clamp(32px, 5.5vw, 56px);
  font-weight: 300;
  font-style: italic;
  color: var(--rose-deep);
  letter-spacing: -0.02em;
  line-height: 1.15;
}
.calm-section { background: linear-gradient(180deg, var(--bg) 0%, var(--paper-soft) 50%, var(--bg) 100%); position: relative; }
.calm-stack { max-width: 640px; margin: 0 auto; position: relative; min-height: 320px; }
.reminder-card {
  background: var(--paper);
  border: 1px solid rgba(140, 118, 96, 0.2);
  border-radius: 8px;
  padding: 50px 44px;
  box-shadow: 0 12px 40px var(--shadow);
  text-align: center;
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  justify-content: center;
  opacity: 0;
  transform: translateY(20px) rotate(-1deg);
  transition: opacity 0.6s ease, transform 0.6s ease;
}
.reminder-card.active { opacity: 1; transform: translateY(0) rotate(0); }
.reminder-stamp {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 12px;
  color: var(--rose);
  letter-spacing: 0.18em;
  margin-bottom: 22px;
  text-transform: uppercase;
}
.reminder-text {
  font-family: 'Fraunces', serif;
  font-size: clamp(22px, 3.5vw, 30px);
  font-weight: 300;
  color: var(--ink);
  line-height: 1.4;
  letter-spacing: -0.01em;
}
.reminder-text em { font-style: italic; color: var(--rose-deep); font-weight: 400; }
.calm-controls { display: flex; justify-content: center; gap: 14px; margin-top: 36px; }
.calm-arrow {
  width: 44px;
  height: 44px;
  border-radius: 50%;
  background: var(--paper);
  border: 1px solid var(--ink-faded);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 18px;
  color: var(--ink-soft);
  transition: all 0.3s ease;
  font-family: serif;
}
.calm-arrow:hover { background: var(--rose); color: var(--paper); border-color: var(--rose); }
.calm-counter {
  display: flex;
  align-items: center;
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 13px;
  color: var(--ink-faded);
  padding: 0 14px;
}
.memory-section { background: var(--bg-warm); padding-bottom: 140px; }
.memory-wall {
  max-width: 1100px;
  margin: 0 auto;
  position: relative;
  display: grid;
  grid-template-columns: repeat(12, 1fr);
  gap: 24px;
  align-items: start;
  padding: 30px 0;
}
.polaroid {
  background: var(--paper);
  padding: 14px 14px 50px;
  box-shadow: 0 12px 30px var(--shadow-deep), 0 1px 0 rgba(255,255,255,0.5) inset;
  border-radius: 2px;
  cursor: pointer;
  transition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.4s ease;
  position: relative;
}
.polaroid:hover { transform: rotate(0deg) scale(1.04) !important; box-shadow: 0 20px 50px var(--shadow-deep); z-index: 5; }
.polaroid img { width: 100%; display: block; background: var(--ink-faded); aspect-ratio: 1 / 1; object-fit: cover; }
.polaroid-caption {
  position: absolute;
  bottom: 14px;
  left: 14px;
  right: 14px;
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 13px;
  color: var(--ink-soft);
  text-align: center;
  letter-spacing: 0.02em;
}
.polaroid:nth-child(1) { grid-column: 1 / span 4; transform: rotate(-3deg); margin-top: 20px; }
.polaroid:nth-child(2) { grid-column: 5 / span 4; transform: rotate(2deg); margin-top: 60px; }
.polaroid:nth-child(3) { grid-column: 9 / span 4; transform: rotate(-2deg); }
.polaroid:nth-child(4) { grid-column: 3 / span 4; transform: rotate(3deg); margin-top: 40px; }
@media (max-width: 720px) {
  .memory-wall { grid-template-columns: 1fr 1fr; gap: 16px; }
  .polaroid { grid-column: span 1 !important; margin-top: 0 !important; }
  .polaroid:nth-child(2n) { transform: rotate(2deg); }
  .polaroid:nth-child(2n+1) { transform: rotate(-2deg); margin-top: 24px !important; }
}
.lightbox {
  position: fixed;
  inset: 0;
  background: rgba(20, 12, 6, 0.92);
  display: none;
  align-items: center;
  justify-content: center;
  z-index: 200;
  padding: 40px;
  cursor: pointer;
}
.lightbox.open { display: flex; }
.lightbox img { max-width: 95%; max-height: 90vh; border-radius: 4px; box-shadow: 0 30px 80px rgba(0,0,0,0.5); }
.reset-section { background: linear-gradient(180deg, var(--bg-warm) 0%, var(--paper) 50%, var(--bg) 100%); }
.reset-flow { max-width: 640px; margin: 0 auto; position: relative; }
.reset-step {
  background: var(--paper);
  border: 1px solid rgba(140, 118, 96, 0.18);
  border-radius: 8px;
  padding: 50px 44px;
  box-shadow: 0 8px 28px var(--shadow);
  display: none;
  text-align: center;
}
.reset-step.active { display: block; animation: fadeUp 0.6s ease-out; }
.reset-step-number {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 13px;
  color: var(--rose);
  letter-spacing: 0.22em;
  margin-bottom: 16px;
  text-transform: uppercase;
}
.reset-step-prompt { font-family: 'Fraunces', serif; font-size: 26px; font-weight: 300; color: var(--ink); line-height: 1.4; margin-bottom: 30px; }
.reset-step-prompt em { font-style: italic; color: var(--rose-deep); }
.mini-breath-stage { width: 130px; height: 130px; margin: 24px auto 36px; display: flex; align-items: center; justify-content: center; }
.mini-breath-circle {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  background: radial-gradient(circle at 30% 30%, var(--rose-soft), var(--rose) 70%);
  box-shadow: 0 8px 24px var(--shadow-warm);
  animation: miniBreath 8s ease-in-out infinite;
}
@keyframes miniBreath {
  0%, 100% { transform: scale(0.55); }
  50% { transform: scale(1); }
}
.reset-promise-display {
  font-family: 'Fraunces', serif;
  text-align: center;
  margin: 8px 0 36px;
  padding: 38px 20px;
  background: var(--paper-soft);
  border-radius: 4px;
  position: relative;
}
.reset-promise-display::before, .reset-promise-display::after {
  content: '';
  position: absolute;
  width: 30px;
  height: 1px;
  background: var(--rose);
  top: 50%;
}
.reset-promise-display::before { left: 30px; }
.reset-promise-display::after { right: 30px; }
.reset-promise-display p { font-size: 32px; font-weight: 300; color: var(--ink); line-height: 1.4; }
.reset-promise-display p em { font-style: italic; color: var(--rose-deep); }
.btn-choose {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 22px;
  padding: 18px 42px;
  background: var(--rose);
  color: var(--paper);
  border: none;
  border-radius: 100px;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 8px 24px var(--shadow-warm);
  font-weight: 400;
  display: inline-block;
  margin: 16px auto 0;
  letter-spacing: 0.01em;
}
.btn-choose:hover { transform: translateY(-2px) scale(1.03); background: var(--rose-deep); box-shadow: 0 12px 30px var(--shadow-warm); }
.btn-choose:active { transform: scale(0.97); }
.reset-step-actions { display: flex; gap: 10px; justify-content: space-between; align-items: center; margin-top: 14px; }
.reset-progress { display: flex; gap: 8px; justify-content: center; margin-bottom: 30px; }
.reset-dot { width: 8px; height: 8px; border-radius: 50%; background: var(--ink-faded); opacity: 0.4; transition: all 0.3s ease; }
.reset-dot.active { background: var(--rose); opacity: 1; transform: scale(1.3); }
.reset-complete {
  text-align: center;
  padding: 60px 30px;
  background: var(--paper);
  border-radius: 8px;
  box-shadow: 0 8px 28px var(--shadow);
  display: none;
  border: 1px solid rgba(140, 118, 96, 0.18);
}
.reset-complete.visible { display: block; animation: fadeUp 0.8s ease-out; }
.reset-complete-mark { font-family: 'Fraunces', serif; font-style: italic; font-size: 60px; color: var(--rose); margin-bottom: 16px; font-weight: 300; letter-spacing: 0.3em; }
.reset-complete-text { font-family: 'Fraunces', serif; font-size: 28px; color: var(--ink); line-height: 1.4; margin-bottom: 18px; font-weight: 300; }
.reset-complete-text em { font-style: italic; color: var(--rose-deep); }
.reset-complete-sub { font-family: 'DM Sans', sans-serif; font-size: 11px; letter-spacing: 0.28em; text-transform: uppercase; color: var(--ink-faded); margin-bottom: 32px; }
.together-section { background: var(--bg); text-align: center; padding-bottom: 80px; }
.heart-button {
  width: 220px;
  height: 220px;
  border-radius: 50%;
  background: radial-gradient(circle at 35% 30%, var(--rose-soft), var(--rose) 60%, var(--rose-deep) 100%);
  border: none;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 30px auto;
  box-shadow: 0 20px 60px var(--shadow-warm);
  position: relative;
  transition: transform 0.3s ease;
}
.heart-button:hover { transform: scale(1.04); }
.heart-button:active { transform: scale(0.96); }
.heart-button::before {
  content: '';
  position: absolute;
  inset: -6px;
  border-radius: 50%;
  border: 1px solid var(--rose-soft);
  opacity: 0.6;
  animation: heartPulseRing 3s ease-in-out infinite;
}
.heart-button::after {
  content: '';
  position: absolute;
  inset: -16px;
  border-radius: 50%;
  border: 1px solid var(--rose-soft);
  opacity: 0.3;
  animation: heartPulseRing 3s 0.5s ease-in-out infinite;
}
@keyframes heartPulseRing {
  0%, 100% { transform: scale(1); opacity: 0.6; }
  50% { transform: scale(1.1); opacity: 0; }
}
.heart-icon { width: 90px; height: 90px; fill: var(--paper); filter: drop-shadow(0 2px 6px rgba(146, 64, 47, 0.4)); }
.heart-button.pulsing { animation: heartBeat 0.8s ease; }
@keyframes heartBeat {
  0%, 100% { transform: scale(1); }
  20% { transform: scale(1.15); }
  40% { transform: scale(0.95); }
  60% { transform: scale(1.1); }
}
.together-message { font-family: 'Fraunces', serif; font-style: italic; font-size: 22px; color: var(--ink); margin-top: 30px; min-height: 36px; transition: opacity 0.4s ease; padding: 0 20px; }
.together-log {
  max-width: 540px;
  margin: 50px auto 0;
  text-align: left;
  background: var(--paper);
  border: 1px solid rgba(140, 118, 96, 0.18);
  border-radius: 6px;
  padding: 28px 32px;
  box-shadow: 0 4px 18px var(--shadow);
}
.together-log-title { font-family: 'DM Sans', sans-serif; font-size: 11px; text-transform: uppercase; letter-spacing: 0.24em; color: var(--ink-faded); margin-bottom: 18px; text-align: center; }
.moment-list { list-style: none; max-height: 240px; overflow-y: auto; }
.moment-list::-webkit-scrollbar { width: 4px; }
.moment-list::-webkit-scrollbar-track { background: transparent; }
.moment-list::-webkit-scrollbar-thumb { background: var(--rose-soft); border-radius: 4px; }
.moment-item {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 15px;
  color: var(--ink-soft);
  padding: 10px 0;
  border-bottom: 1px dashed rgba(140, 118, 96, 0.2);
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 14px;
}
.moment-item:last-child { border-bottom: none; }
.moment-item .when { font-size: 12px; letter-spacing: 0.06em; color: var(--ink-faded); font-style: normal; }
.moment-item .heart-mini { color: var(--rose); font-size: 13px; flex-shrink: 0; }
.moment-empty { font-family: 'Fraunces', serif; font-style: italic; color: var(--ink-faded); text-align: center; font-size: 15px; padding: 12px 0; }

/* ============ UPSET / MOOD METER ============ */
.upset-section {
  background: linear-gradient(180deg, var(--bg) 0%, var(--bg-warm) 100%);
  padding-bottom: 100px;
}
.upset-sticker-wrap {
  display: flex;
  justify-content: center;
  padding: 0 20px;
}
.upset-sticker {
  position: relative;
  background: var(--paper);
  border: 1px solid rgba(140, 118, 96, 0.25);
  border-radius: 3px;
  padding: 60px 52px 50px;
  max-width: 540px;
  width: 100%;
  text-align: center;
  box-shadow:
    0 20px 60px var(--shadow-deep),
    4px 4px 0 rgba(193, 144, 86, 0.15),
    -2px -2px 0 rgba(184, 92, 78, 0.08);
  transition: background 0.6s ease;
}
/* tape strip at top */
.upset-sticker::before {
  content: '';
  position: absolute;
  top: -13px;
  left: 50%;
  transform: translateX(-50%) rotate(-1deg);
  width: 88px;
  height: 26px;
  background: rgba(227, 192, 140, 0.55);
  border-radius: 2px;
  box-shadow: 0 1px 4px rgba(58,40,28,0.1);
}
.upset-emoji-ring {
  width: 120px;
  height: 120px;
  border-radius: 50%;
  background: var(--paper-soft);
  border: 2px solid rgba(184, 92, 78, 0.2);
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 24px;
  transition: border-color 0.5s ease, background 0.5s ease;
  box-shadow: 0 6px 20px var(--shadow);
}
.upset-emoji {
  font-size: 58px;
  line-height: 1;
  display: block;
  transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  user-select: none;
}
.upset-percent-badge {
  font-family: 'Fraunces', serif;
  font-size: clamp(52px, 10vw, 80px);
  font-weight: 700;
  color: var(--rose);
  line-height: 1;
  letter-spacing: -0.04em;
  margin-bottom: 12px;
  transition: color 0.5s ease;
}
.upset-mood-label {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: clamp(19px, 2.8vw, 26px);
  font-weight: 400;
  color: var(--ink);
  margin-bottom: 10px;
  letter-spacing: -0.01em;
}
.upset-mood-sub {
  font-family: 'DM Sans', sans-serif;
  font-size: 14px;
  color: var(--ink-faded);
  letter-spacing: 0.04em;
  margin-bottom: 40px;
  line-height: 1.55;
  min-height: 44px;
  transition: opacity 0.25s ease;
}
.upset-slider-area { width: 100%; }
.upset-scale-emojis {
  display: flex;
  justify-content: space-between;
  font-size: 18px;
  margin-bottom: 10px;
  padding: 0 2px;
  user-select: none;
  line-height: 1;
}
.upset-slider {
  -webkit-appearance: none;
  appearance: none;
  width: 100%;
  height: 7px;
  border-radius: 100px;
  background: linear-gradient(to right,
    #7a8a6f 0%,
    #c19056 30%,
    #b85c4e 65%,
    #92402f 100%);
  cursor: pointer;
  outline: none;
  margin: 0 0 14px;
  display: block;
}
.upset-slider::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: 30px;
  height: 30px;
  border-radius: 50%;
  background: var(--paper);
  border: 2.5px solid var(--rose);
  cursor: grab;
  box-shadow: 0 3px 10px var(--shadow-deep), 0 0 0 5px rgba(184, 92, 78, 0.1);
  transition: transform 0.2s ease, border-color 0.4s ease;
}
.upset-slider::-webkit-slider-thumb:active {
  cursor: grabbing;
  transform: scale(1.18);
  box-shadow: 0 5px 18px var(--shadow-deep), 0 0 0 8px rgba(184, 92, 78, 0.14);
}
.upset-slider::-moz-range-thumb {
  width: 30px;
  height: 30px;
  border-radius: 50%;
  background: var(--paper);
  border: 2.5px solid var(--rose);
  cursor: grab;
  box-shadow: 0 3px 10px var(--shadow-deep);
}
.upset-scale-words {
  display: flex;
  justify-content: space-between;
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 0.2em;
  color: var(--ink-faded);
  padding: 0 2px;
  font-family: 'DM Sans', sans-serif;
}
@media (max-width: 540px) {
  .upset-sticker { padding: 52px 28px 40px; }
  .upset-scale-words { font-size: 9px; letter-spacing: 0.12em; }
}

footer {
  text-align: center;
  padding: 50px 6vw 40px;
  background: var(--ink);
  color: var(--paper-soft);
}
footer .promise-mini { font-family: 'Fraunces', serif; font-style: italic; font-size: 22px; color: var(--rose-soft); margin-bottom: 14px; }
footer .promise-mini em { color: var(--gold-soft); }
footer p { font-size: 12px; letter-spacing: 0.18em; text-transform: uppercase; color: rgba(245, 236, 217, 0.5); }
footer .signature { font-family: 'Fraunces', serif; font-style: italic; font-size: 14px; color: var(--rose-soft); margin-top: 18px; letter-spacing: 0.04em; text-transform: none; opacity: 0.85; }
.fade-section { opacity: 0; transform: translateY(30px); transition: opacity 1s ease-out, transform 1s ease-out; }
.fade-section.visible { opacity: 1; transform: translateY(0); }
@media (max-width: 540px) {
  section { padding: 70px 5vw; }
  .reset-step { padding: 36px 26px; }
  .reset-promise-display p { font-size: 26px; }
}

/* ============ SHOOTING STAR / BROKEN HEART FLARE ============ */
.star-overlay {
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 9999;
  overflow: hidden;
}
.star-flair {
  position: absolute;
  top: -90px;
  right: -90px;
  display: flex;
  align-items: center;
  animation: starFlight 1.9s cubic-bezier(0.28, 0.02, 0.72, 1) forwards;
  will-change: transform, opacity;
}
@keyframes starFlight {
  0%   { transform: translate(0, 0);                                    opacity: 0; }
  5%   { opacity: 1; }
  80%  { opacity: 1; }
  100% { transform: translate(calc(-100vw - 180px), calc(100vh + 180px)); opacity: 0; }
}
/* wide soft glow behind the whole tail */
.star-glow-halo {
  position: absolute;
  top: 50%;
  right: -30px;
  transform: translateY(-50%);
  width: 380px;
  height: 40px;
  border-radius: 20px;
  background: linear-gradient(90deg,
    transparent 0%,
    rgba(184, 92, 78, 0.08) 30%,
    rgba(240, 150, 130, 0.22) 70%,
    rgba(255, 200, 180, 0.35) 100%
  );
  filter: blur(8px);
  pointer-events: none;
}
/* tight bright core tail */
.star-tail {
  width: 240px;
  height: 5px;
  border-radius: 3px;
  background: linear-gradient(90deg,
    transparent 0%,
    rgba(184, 92, 78, 0.25) 25%,
    rgba(210, 110, 90, 0.7) 60%,
    rgba(255, 210, 190, 1) 100%
  );
  margin-right: -3px;
  filter: blur(0.4px);
  flex-shrink: 0;
}
/* thin bright highlight along the tail */
.star-tail::before {
  content: '';
  position: absolute;
  top: 50%;
  right: 0;
  transform: translateY(-50%);
  width: 100px;
  height: 1.5px;
  border-radius: 1px;
  background: linear-gradient(90deg, transparent, rgba(255, 240, 230, 0.9));
}
/* the broken heart */
.star-heart {
  font-size: 50px;
  line-height: 1;
  position: relative;
  z-index: 2;
  flex-shrink: 0;
  filter:
    drop-shadow(0 0 6px rgba(184, 92, 78, 1))
    drop-shadow(0 0 16px rgba(184, 92, 78, 0.85))
    drop-shadow(0 0 40px rgba(220, 100, 80, 0.55))
    drop-shadow(0 0 80px rgba(240, 120, 100, 0.3));
  animation: heartFlicker 0.12s ease-in-out infinite alternate;
}
@keyframes heartFlicker {
  from {
    filter:
      drop-shadow(0 0 6px rgba(184, 92, 78, 1))
      drop-shadow(0 0 18px rgba(184, 92, 78, 0.8))
      drop-shadow(0 0 42px rgba(220, 100, 80, 0.5));
    transform: scale(1);
  }
  to {
    filter:
      drop-shadow(0 0 10px rgba(220, 60, 40, 1))
      drop-shadow(0 0 28px rgba(220, 60, 40, 0.9))
      drop-shadow(0 0 60px rgba(255, 90, 70, 0.55))
      drop-shadow(0 0 100px rgba(255, 120, 90, 0.25));
    transform: scale(1.1);
  }
}
/* ember sparks — tiny dots that peel off */
.star-ember {
  position: absolute;
  border-radius: 50%;
  pointer-events: none;
  animation: emberDrift var(--dur, 0.7s) ease-out forwards;
}
@keyframes emberDrift {
  0%   { transform: translate(0, 0) scale(1); opacity: 0.9; }
  100% { transform: translate(var(--dx, -20px), var(--dy, -30px)) scale(0); opacity: 0; }
}
</style>
</head>
<body>

<nav>
  <div class="nav-mark">we talk · we fix · we stay</div>
  <div class="nav-links">
    <a href="#pause">pause</a>
    <a href="#vow">vow</a>
    <a href="#calm">calm</a>
    <a href="#memories">memories</a>
    <a href="#reset">reset</a>
    <a href="#together">together</a>
    <a href="#mood">mood</a>
  </div>
</nav>

<header class="hero" id="home">
  <svg class="hero-decoration" viewBox="0 0 200 200" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
    <circle cx="100" cy="100" r="80" stroke="#b85c4e" stroke-width="0.5" stroke-dasharray="2 4" opacity="0.5"/>
    <circle cx="100" cy="100" r="50" stroke="#c19056" stroke-width="0.5" opacity="0.4"/>
    <path d="M100 30 L100 170 M30 100 L170 100" stroke="#8c7660" stroke-width="0.3" opacity="0.3"/>
  </svg>
  <svg class="hero-decoration-2" viewBox="0 0 140 80" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
    <path d="M5 40 Q 35 5, 70 40 T 135 40" stroke="#b85c4e" stroke-width="1" fill="none"/>
    <path d="M5 50 Q 35 15, 70 50 T 135 50" stroke="#c19056" stroke-width="0.6" fill="none" opacity="0.6"/>
  </svg>
  <div class="hero-inner">
    <p class="eyebrow">a quiet place for us</p>
    <h1 class="promise">
      <span>we talk.</span>
      <span>we fix.</span>
      <span>we stay.</span>
    </h1>
    <div class="clocks">
      <div class="clock">
        <div class="clock-stamp">him</div>
        <p class="clock-city">Lahore, PK</p>
        <p class="clock-name" id="himName">—</p>
        <svg class="clock-analog" viewBox="0 0 200 200" id="lahoreClock" aria-label="Lahore time">
          <circle class="clock-face" cx="100" cy="100" r="92"/>
          <circle class="clock-face-inner" cx="100" cy="100" r="78"/>
          <g class="ticks" data-ticks></g>
          <text class="clock-numeral" x="100" y="28">12</text>
          <text class="clock-numeral" x="172" y="100">3</text>
          <text class="clock-numeral" x="100" y="174">6</text>
          <text class="clock-numeral" x="28" y="100">9</text>
          <line class="hour-hand" x1="100" y1="100" x2="100" y2="55" id="lahoreHour"/>
          <line class="minute-hand" x1="100" y1="100" x2="100" y2="36" id="lahoreMinute"/>
          <line class="second-hand" x1="100" y1="108" x2="100" y2="32" id="lahoreSecond"/>
          <circle class="clock-center" cx="100" cy="100" r="3.5"/>
          <circle class="clock-pin" cx="100" cy="100" r="1.4"/>
        </svg>
        <p class="clock-meta"><span class="status-dot" id="lahoreStatus"></span><span id="lahoreStatusText">—</span></p>
      </div>
      <div class="between">
        <span>across</span>
        <span class="between-line"></span>
        <span class="between-distance">~5,800 km</span>
        <span class="between-line"></span>
        <span>between</span>
        <span class="between-tz">she's 3hrs ahead</span>
      </div>
      <div class="clock">
        <div class="clock-stamp">her</div>
        <p class="clock-city">Caraga, PH</p>
        <p class="clock-name" id="herName">—</p>
        <svg class="clock-analog" viewBox="0 0 200 200" id="caragaClock" aria-label="Caraga time">
          <circle class="clock-face" cx="100" cy="100" r="92"/>
          <circle class="clock-face-inner" cx="100" cy="100" r="78"/>
          <g class="ticks" data-ticks></g>
          <text class="clock-numeral" x="100" y="28">12</text>
          <text class="clock-numeral" x="172" y="100">3</text>
          <text class="clock-numeral" x="100" y="174">6</text>
          <text class="clock-numeral" x="28" y="100">9</text>
          <line class="hour-hand" x1="100" y1="100" x2="100" y2="55" id="caragaHour"/>
          <line class="minute-hand" x1="100" y1="100" x2="100" y2="36" id="caragaMinute"/>
          <line class="second-hand" x1="100" y1="108" x2="100" y2="32" id="caragaSecond"/>
          <circle class="clock-center" cx="100" cy="100" r="3.5"/>
          <circle class="clock-pin" cx="100" cy="100" r="1.4"/>
        </svg>
        <p class="clock-meta"><span class="status-dot" id="caragaStatus"></span><span id="caragaStatusText">—</span></p>
      </div>
    </div>
    <p class="days-counter">
      <strong id="dayCount">—</strong> days of choosing each other.
      <span class="days-counter-sub">they said it wouldn't last. day one was the answer. so is today.</span>
    </p>
  </div>
</header>

<section class="pause-section fade-section" id="pause">
  <div class="section-header">
    <p class="section-number">— 01 —</p>
    <h2 class="section-title">the <em>pause</em> room.</h2>
    <p class="section-tag">when it's getting loud inside. before we say the thing we don't mean.</p>
  </div>
  <div class="pause-content">
    <div class="breath-stage">
      <div class="breath-circle" id="breathCircle"></div>
      <span class="breath-text" id="breathText">tap to begin</span>
    </div>
    <div class="pause-controls">
      <p class="pause-timer" id="pauseTimer">10:00</p>
      <button class="btn" id="pauseBtn">begin</button>
    </div>
    <p class="pause-prompt">come back when you can hear them.</p>
  </div>
</section>

<section class="vow-section fade-section" id="vow">
  <div class="section-header">
    <p class="section-number">— 02 —</p>
    <h2 class="section-title">the <em>vow</em>.</h2>
    <p class="section-tag">what we already know. what they don't see. what we keep choosing, fight after fight.</p>
  </div>
  <div class="vow-list">
    <div class="vow-item">
      <p class="vow-text">it is never <strong>me versus you</strong>.<br>it is always <em>us versus the world</em>.</p>
    </div>
    <div class="vow-divider">
      <svg viewBox="0 0 80 14" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <line x1="0" y1="7" x2="32" y2="7" stroke="#8c7660" stroke-width="0.6"/>
        <circle cx="40" cy="7" r="3" fill="#b85c4e"/>
        <line x1="48" y1="7" x2="80" y2="7" stroke="#8c7660" stroke-width="0.6"/>
      </svg>
    </div>
    <div class="vow-item">
      <p class="vow-text">they said it wouldn't work.<br>we accepted that as a <em>challenge</em>.<br>not a verdict.</p>
    </div>
    <div class="vow-divider">
      <svg viewBox="0 0 80 14" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <line x1="0" y1="7" x2="32" y2="7" stroke="#8c7660" stroke-width="0.6"/>
        <circle cx="40" cy="7" r="3" fill="#b85c4e"/>
        <line x1="48" y1="7" x2="80" y2="7" stroke="#8c7660" stroke-width="0.6"/>
      </svg>
    </div>
    <div class="vow-item">
      <p class="vow-text">distance is <em>geography</em>.<br>the bond is <strong>not</strong> geography.</p>
    </div>
    <div class="vow-divider">
      <svg viewBox="0 0 80 14" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <line x1="0" y1="7" x2="32" y2="7" stroke="#8c7660" stroke-width="0.6"/>
        <circle cx="40" cy="7" r="3" fill="#b85c4e"/>
        <line x1="48" y1="7" x2="80" y2="7" stroke="#8c7660" stroke-width="0.6"/>
      </svg>
    </div>
    <div class="vow-item">
      <p class="vow-text">every fight is just two people on the <strong>same side</strong>,<br>forgetting <em>briefly</em> that they're on the same side.</p>
    </div>
    <div class="vow-divider">
      <svg viewBox="0 0 80 14" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <line x1="0" y1="7" x2="32" y2="7" stroke="#8c7660" stroke-width="0.6"/>
        <circle cx="40" cy="7" r="3" fill="#b85c4e"/>
        <line x1="48" y1="7" x2="80" y2="7" stroke="#8c7660" stroke-width="0.6"/>
      </svg>
    </div>
    <div class="vow-item">
      <p class="vow-text">when one of us breaks,<br>the other <em>carries both</em>.<br>that's the agreement.</p>
    </div>
    <div class="vow-divider">
      <svg viewBox="0 0 80 14" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <line x1="0" y1="7" x2="32" y2="7" stroke="#8c7660" stroke-width="0.6"/>
        <circle cx="40" cy="7" r="3" fill="#b85c4e"/>
        <line x1="48" y1="7" x2="80" y2="7" stroke="#8c7660" stroke-width="0.6"/>
      </svg>
    </div>
    <div class="vow-item">
      <p class="vow-text">we did not <em>fall</em> into this.<br>we walked in. <strong>eyes open</strong>. on purpose.</p>
    </div>
  </div>
  <div class="vow-closing">
    <p class="vow-closing-mark">— and we keep walking —</p>
    <p class="vow-closing-text">we chose us.<br>we keep choosing <em>us</em>.</p>
  </div>
</section>

<section class="calm-section fade-section" id="calm">
  <div class="section-header">
    <p class="section-number">— 03 —</p>
    <h2 class="section-title">when the head <em>spirals</em>.</h2>
    <p class="section-tag">letters from the calm version of you, to the panicking one.</p>
  </div>
  <div class="calm-stack" id="calmStack"></div>
  <div class="calm-controls">
    <button class="calm-arrow" id="calmPrev" aria-label="previous">‹</button>
    <span class="calm-counter" id="calmCounter">— · —</span>
    <button class="calm-arrow" id="calmNext" aria-label="next">›</button>
  </div>
</section>

<section class="memory-section fade-section" id="memories">
  <div class="section-header">
    <p class="section-number">— 04 —</p>
    <h2 class="section-title">the <em>memory</em> wall.</h2>
    <p class="section-tag">for the 3am spiral, when you need proof that the good is real.</p>
  </div>
  <div class="memory-wall" id="memoryWall">
    <div class="polaroid" data-src="https://i.ibb.co/gcPbrCd/Love-1.png">
      <img src="https://i.ibb.co/gcPbrCd/Love-1.png" alt="memory" loading="lazy">
      <div class="polaroid-caption">my favorite person</div>
    </div>
    <div class="polaroid" data-src="https://i.ibb.co/939wRSPz/Love-2.png">
      <img src="https://i.ibb.co/939wRSPz/Love-2.png" alt="memory" loading="lazy">
      <div class="polaroid-caption">that smile fixes me</div>
    </div>
    <div class="polaroid" data-src="https://i.ibb.co/GfX6wHft/Love-3.png">
      <img src="https://i.ibb.co/GfX6wHft/Love-3.png" alt="memory" loading="lazy">
      <div class="polaroid-caption">my crazy filipina</div>
    </div>
    <div class="polaroid" data-src="https://i.ibb.co/21q4ds8N/Love-4.png">
      <img src="https://i.ibb.co/21q4ds8N/Love-4.png" alt="memory" loading="lazy">
      <div class="polaroid-caption">my cute pie</div>
    </div>
  </div>
</section>
<div class="lightbox" id="lightbox">
  <img id="lightboxImg" src="" alt="enlarged memory">
</div>

<section class="reset-section fade-section" id="reset">
  <div class="section-header">
    <p class="section-number">— 05 —</p>
    <h2 class="section-title">the <em>reset</em>.</h2>
    <p class="section-tag">after the storm. three small steps to choose us again.</p>
  </div>
  <div class="reset-flow" id="resetFlow">
    <div class="reset-progress">
      <span class="reset-dot active"></span>
      <span class="reset-dot"></span>
      <span class="reset-dot"></span>
    </div>
    <div class="reset-step active" data-step="1">
      <p class="reset-step-number">— step one —</p>
      <p class="reset-step-prompt"><em>pause</em>.<br>just for a moment. let it cool.</p>
      <div class="mini-breath-stage"><div class="mini-breath-circle"></div></div>
      <div class="reset-step-actions">
        <span></span>
        <button class="btn" data-next="2">i'm steadier</button>
      </div>
    </div>
    <div class="reset-step" data-step="2">
      <p class="reset-step-number">— step two —</p>
      <p class="reset-step-prompt">say the promise out loud.<br>alone, or <em>together</em>. but mean it.</p>
      <div class="reset-promise-display">
        <p>we talk.<br><em>we fix.</em><br>we stay.</p>
      </div>
      <div class="reset-step-actions">
        <button class="btn-small" data-prev="1">back</button>
        <button class="btn" data-next="3">we said it</button>
      </div>
    </div>
    <div class="reset-step" data-step="3">
      <p class="reset-step-number">— step three —</p>
      <p class="reset-step-prompt">now <em>choose us</em> again.<br>one more time.<br>like you did the first time.</p>
      <button class="btn-choose" id="chooseBtn">i choose us</button>
      <div class="reset-step-actions" style="margin-top: 36px;">
        <button class="btn-small" data-prev="2">back</button>
        <span></span>
      </div>
    </div>
    <div class="reset-complete" id="resetCompleteMsg">
      <div class="reset-complete-mark">·  ·  ·</div>
      <p class="reset-complete-text">we chose us.<br><em>again.</em><br>like every other time.</p>
      <p class="reset-complete-sub">— the world doesn't get to vote —</p>
      <button class="btn-soft" id="resetRestart">close · for now</button>
    </div>
  </div>
</section>

<section class="together-section fade-section" id="together">
  <div class="section-header">
    <p class="section-number">— 06 —</p>
    <h2 class="section-title"><em>together</em>, even apart.</h2>
    <p class="section-tag">press it when your heart finds them. each press is kept here, like a little proof.</p>
  </div>
  <button class="heart-button" id="heartBtn" aria-label="thinking of you">
    <svg class="heart-icon" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
      <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
    </svg>
  </button>
  <p class="together-message" id="togetherMessage">tap when your heart finds them.</p>
  <div class="together-log">
    <p class="together-log-title">our moments</p>
    <ul class="moment-list" id="momentList">
      <li class="moment-empty">the first one starts here.</li>
    </ul>
  </div>
</section>

<!-- ============ MOOD METER ============ -->
<section class="upset-section fade-section" id="mood">
  <div class="section-header">
    <p class="section-number">— 07 —</p>
    <h2 class="section-title">the <em>mood</em> meter.</h2>
    <p class="section-tag">no judgment here. just slide it to where you actually are right now.</p>
  </div>
  <div class="upset-sticker-wrap">
    <div class="upset-sticker" id="upsetSticker">
      <div class="upset-emoji-ring" id="upsetRing">
        <span class="upset-emoji" id="upsetEmoji">😊</span>
      </div>
      <p class="upset-percent-badge" id="upsetPercent">10%</p>
      <p class="upset-mood-label" id="upsetMoodLabel">barely a ripple</p>
      <p class="upset-mood-sub" id="upsetMoodSub">he's probably trying his best, tbh.</p>
      <div class="upset-slider-area">
        <div class="upset-scale-emojis">
          <span>🥰</span>
          <span>🙂</span>
          <span>😐</span>
          <span>😒</span>
          <span>😠</span>
          <span>😭</span>
        </div>
        <input type="range" class="upset-slider" id="upsetSlider" min="0" max="100" step="10" value="10">
        <div class="upset-scale-words">
          <span>all fine</span>
          <span>somewhere in between</span>
          <span>send help</span>
        </div>
      </div>
    </div>
  </div>
</section>

<footer>
  <p class="promise-mini">we talk · <em>we fix</em> · we stay</p>
  <p>lahore — caraga</p>
  <p class="signature">made with love, on purpose.</p>
</footer>

<script>
const CONFIG = {
  himName: "Your Guy",
  herName: "Vaniza",
  startDate: "2026-02-27",
};

const LAHORE_OFFSET = 5;
const CARAGA_OFFSET = 8;

const $ = (sel) => document.querySelector(sel);
const $$ = (sel) => document.querySelectorAll(sel);

$('#himName').textContent = CONFIG.himName;
$('#herName').textContent = CONFIG.herName;

function buildTicks() {
  $$('[data-ticks]').forEach(group => {
    for (let i = 0; i < 60; i++) {
      const isMajor = i % 5 === 0;
      const tick = document.createElementNS('http://www.w3.org/2000/svg', 'line');
      tick.setAttribute('x1', 100);
      tick.setAttribute('y1', isMajor ? 12 : 14);
      tick.setAttribute('x2', 100);
      tick.setAttribute('y2', isMajor ? 18 : 16);
      tick.setAttribute('class', isMajor ? 'tick major' : 'tick');
      tick.setAttribute('transform', `rotate(${i * 6} 100 100)`);
      group.appendChild(tick);
    }
  });
}
buildTicks();

function timeForOffset(offsetHours) {
  const utcMs = Date.now();
  const localMs = utcMs + offsetHours * 3600 * 1000;
  const date = new Date(localMs);
  const totalSec =
    date.getUTCHours() * 3600 +
    date.getUTCMinutes() * 60 +
    date.getUTCSeconds() +
    date.getUTCMilliseconds() / 1000;
  return {
    hours: (totalSec / 3600) % 24,
    minutes: (totalSec / 60) % 60,
    seconds: totalSec % 60,
    h24: date.getUTCHours(),
  };
}

function setHand(id, deg) {
  const el = document.getElementById(id);
  if (el) el.setAttribute('transform', `rotate(${deg} 100 100)`);
}

function updateClocks() {
  const lahore = timeForOffset(LAHORE_OFFSET);
  const caraga = timeForOffset(CARAGA_OFFSET);
  setHand('lahoreHour', (lahore.hours % 12) * 30);
  setHand('lahoreMinute', lahore.minutes * 6);
  setHand('lahoreSecond', lahore.seconds * 6);
  setHand('caragaHour', (caraga.hours % 12) * 30);
  setHand('caragaMinute', caraga.minutes * 6);
  setHand('caragaSecond', caraga.seconds * 6);
  const hisStatus = getHisStatus(lahore.h24);
  const herStatus = getHerStatus(caraga.h24);
  $('#lahoreStatusText').textContent = hisStatus.text;
  $('#caragaStatusText').textContent = herStatus.text;
  $('#lahoreStatus').classList.toggle('asleep', !hisStatus.active);
  $('#caragaStatus').classList.toggle('asleep', !herStatus.active);
  requestAnimationFrame(updateClocks);
}

function getHisStatus(h) {
  if (h === 0) return { active: true, text: "winding down · she's checking in" };
  if (h >= 1 && h <= 8) return { active: false, text: "asleep · she's already at school" };
  if (h === 9) return { active: true, text: "getting ready · office at ten" };
  if (h >= 10 && h <= 18) return { active: true, text: "at the office · thinking of her" };
  if (h === 19) return { active: true, text: "heading home · day winding down" };
  if (h === 20) return { active: true, text: "home · dinner soon" };
  if (h >= 21 && h <= 23) return { active: true, text: "deep into his project · she's asleep" };
  return { active: true, text: "—" };
}

function getHerStatus(h) {
  if (h >= 22 || h <= 5) return { active: false, text: "asleep · he's working late" };
  if (h >= 6 && h <= 12) return { active: true, text: "at school · teaching her kids" };
  if (h >= 13 && h <= 18) return { active: true, text: "free time · doom scrolling or watching K-drama" };
  if (h >= 19 && h <= 21) return { active: true, text: "chores · winding down soon" };
  return { active: true, text: "—" };
}

updateClocks();

function updateDays() {
  const start = new Date(CONFIG.startDate + 'T00:00:00');
  const now = new Date();
  const days = Math.floor((now - start) / (1000 * 60 * 60 * 24));
  $('#dayCount').textContent = days >= 0 ? days.toLocaleString() : '0';
}
updateDays();
setInterval(updateDays, 60 * 60 * 1000);

// PAUSE ROOM
const breathCircle = $('#breathCircle');
const breathText = $('#breathText');
const pauseBtn = $('#pauseBtn');
const pauseTimer = $('#pauseTimer');
let pauseActive = false;
let pauseInterval = null;
let pauseSecondsLeft = 600;
let breathPhase = 0;
let breathTimeout = null;
const breathPhases = [
  { label: 'breathe in', cls: 'in', dur: 4000 },
  { label: 'hold', cls: 'hold', dur: 2000 },
  { label: 'breathe out', cls: 'out', dur: 6000 },
  { label: 'hold', cls: 'hold', dur: 1000 },
];

function runBreath() {
  if (!pauseActive) return;
  const phase = breathPhases[breathPhase];
  breathCircle.classList.remove('in', 'hold', 'out');
  breathCircle.classList.add(phase.cls);
  breathText.textContent = phase.label;
  breathTimeout = setTimeout(() => {
    breathPhase = (breathPhase + 1) % breathPhases.length;
    runBreath();
  }, phase.dur);
}

function fmtTime(s) {
  const m = Math.floor(s / 60);
  const sec = s % 60;
  return `${String(m).padStart(2, '0')}:${String(sec).padStart(2, '0')}`;
}

function tickPause() {
  pauseSecondsLeft--;
  pauseTimer.textContent = fmtTime(pauseSecondsLeft);
  if (pauseSecondsLeft <= 0) {
    stopPause();
    breathText.textContent = 'go talk to them, gently';
    setTimeout(() => { breathText.textContent = 'tap to begin'; }, 6000);
  }
}

function startPause() {
  pauseActive = true;
  pauseSecondsLeft = 600;
  breathPhase = 0;
  pauseTimer.textContent = fmtTime(pauseSecondsLeft);
  pauseBtn.textContent = 'end early';
  runBreath();
  pauseInterval = setInterval(tickPause, 1000);
}

function stopPause() {
  pauseActive = false;
  clearTimeout(breathTimeout);
  clearInterval(pauseInterval);
  pauseBtn.textContent = 'begin';
  breathCircle.classList.remove('in', 'hold');
  breathCircle.classList.add('out');
  pauseTimer.textContent = '10:00';
}

pauseBtn.addEventListener('click', () => { if (pauseActive) stopPause(); else startPause(); });
breathCircle.addEventListener('click', () => { if (!pauseActive) startPause(); });

// OVERTHINKING REMINDERS
const reminders = [
  { stamp: 'when the head says "this won\'t last"', text: "they bet against us.<br>we bet on <em>each other</em>.<br>that's the only score that matters." },
  { stamp: 'when the silence stretches', text: "silence isn't absence.<br>they're just on their side of the world,<br><em>breathing</em>." },
  { stamp: 'when the worst case takes over', text: "the worst case is a <em>story</em><br>your fear is telling.<br>the real thing is the person who keeps showing up." },
  { stamp: 'when someone outside doubts', text: "their doubt is information about <em>them</em>.<br>your choosing is information about <em>you</em>." },
  { stamp: 'when the distance feels heavy', text: "you accepted this challenge.<br>you both did. <em>eyes open</em>.<br>you don't get to forget that on the bad days." },
  { stamp: 'when you forget you\'re on the same side', text: "it was never <em>me vs you</em>.<br>it was always us vs the world.<br>find your way back to the same side." },
  { stamp: 'tonight', text: "they're three hours away on the same earth,<br>choosing the <em>same thing</em> you are.<br>that's not nothing. that's everything." },
];
let currentReminderIdx = 0;

function renderReminders() {
  const stack = $('#calmStack');
  stack.innerHTML = '';
  reminders.forEach((r, i) => {
    const card = document.createElement('div');
    card.className = 'reminder-card' + (i === currentReminderIdx ? ' active' : '');
    card.innerHTML = `<p class="reminder-stamp">${r.stamp}</p><p class="reminder-text">${r.text}</p>`;
    stack.appendChild(card);
  });
  updateCalmCounter();
}

function updateCalmCounter() {
  $('#calmCounter').textContent = `${currentReminderIdx + 1} · ${reminders.length}`;
}

function changeReminder(dir) {
  currentReminderIdx = (currentReminderIdx + dir + reminders.length) % reminders.length;
  $$('.reminder-card').forEach((c, i) => c.classList.toggle('active', i === currentReminderIdx));
  updateCalmCounter();
}

$('#calmPrev').addEventListener('click', () => changeReminder(-1));
$('#calmNext').addEventListener('click', () => changeReminder(1));
renderReminders();

// MEMORY WALL
$$('.polaroid').forEach(p => {
  p.addEventListener('click', () => {
    $('#lightboxImg').src = p.dataset.src;
    $('#lightbox').classList.add('open');
  });
});
$('#lightbox').addEventListener('click', () => { $('#lightbox').classList.remove('open'); });

// RESET
function showResetStep(n) {
  $$('.reset-step').forEach(s => s.classList.remove('active'));
  const target = document.querySelector(`.reset-step[data-step="${n}"]`);
  if (target) target.classList.add('active');
  $$('.reset-dot').forEach((d, i) => d.classList.toggle('active', i < n));
}

$$('[data-next]').forEach(b => b.addEventListener('click', () => showResetStep(parseInt(b.dataset.next))));
$$('[data-prev]').forEach(b => b.addEventListener('click', () => showResetStep(parseInt(b.dataset.prev))));

$('#chooseBtn').addEventListener('click', () => {
  $$('.reset-step').forEach(s => s.classList.remove('active'));
  document.querySelector('.reset-progress').style.display = 'none';
  $('#resetCompleteMsg').classList.add('visible');
  recordMoment(true);
});

$('#resetRestart').addEventListener('click', () => {
  $('#resetCompleteMsg').classList.remove('visible');
  document.querySelector('.reset-progress').style.display = 'flex';
  showResetStep(1);
});

// TOGETHER MOMENT
const heartBtn = $('#heartBtn');
const togetherMsg = $('#togetherMessage');
const momentList = $('#momentList');
const togetherMessages = [
  "kept · saved · sent across the sky.",
  "this counts. this is staying.",
  "across all that distance, this lands.",
  "they're somewhere right now. so are you. that's enough.",
  "your heart pulse was felt.",
  "a small proof. a real one.",
  "us versus the world · still winning.",
];

function fmtMoment(d) {
  return d.toLocaleString('en-US', {
    month: 'short', day: 'numeric',
    hour: 'numeric', minute: '2-digit', hour12: true
  }).toLowerCase();
}

function renderMoments() {
  const moments = JSON.parse(localStorage.getItem('moments') || '[]');
  if (moments.length === 0) {
    momentList.innerHTML = '<li class="moment-empty">the first one starts here.</li>';
    return;
  }
  momentList.innerHTML = moments.slice(-12).reverse().map(m => {
    const d = new Date(m.at);
    const tag = m.kind === 'choose' ? '· chose us' : '';
    return `<li class="moment-item"><span class="heart-mini">♡</span><span class="when">${fmtMoment(d)} ${tag}</span></li>`;
  }).join('');
}

function recordMoment(isChoose) {
  const moments = JSON.parse(localStorage.getItem('moments') || '[]');
  moments.push({ at: new Date().toISOString(), kind: isChoose ? 'choose' : 'tap' });
  localStorage.setItem('moments', JSON.stringify(moments));
  renderMoments();
}

renderMoments();

heartBtn.addEventListener('click', () => {
  heartBtn.classList.remove('pulsing');
  void heartBtn.offsetWidth;
  heartBtn.classList.add('pulsing');
  recordMoment(false);
  const msg = togetherMessages[Math.floor(Math.random() * togetherMessages.length)];
  togetherMsg.style.opacity = '0';
  setTimeout(() => {
    togetherMsg.textContent = msg;
    togetherMsg.style.opacity = '1';
  }, 200);
});

// ============================================
// MOOD METER — UPSET SLIDER
// ============================================
const MOODS = {
  0:   { emoji: '🥰', label: 'absolutely not upset',        sub: 'we\'re literally perfect right now.' },
  10:  { emoji: '😊', label: 'barely a ripple',             sub: 'he\'s probably trying his best, tbh.' },
  20:  { emoji: '🙂', label: 'something\'s a little off',   sub: 'it\'ll pass. it always does.' },
  30:  { emoji: '😐', label: 'hmm. noted.',                 sub: 'a quiet kind of bothered.' },
  40:  { emoji: '😑', label: 'he did that thing again',     sub: 'you know the one.' },
  50:  { emoji: '😒', label: 'in that in-between place',    sub: 'not quite fine. not quite not fine.' },
  60:  { emoji: '😤', label: 'genuinely annoyed',           sub: 'still love him. just... annoyed.' },
  70:  { emoji: '😠', label: 'silent treatment activated',  sub: 'he should probably reach out first.' },
  80:  { emoji: '😡', label: 'okay this is serious',        sub: 'we need to actually talk.' },
  90:  { emoji: '😢', label: 'hurt. really hurt.',          sub: 'please be gentle when you come back.' },
  100: { emoji: '😭', label: 'full breakdown mode',         sub: 'drop everything. she needs you now.' },
};

const upsetSlider   = document.getElementById('upsetSlider');
const upsetEmojiEl  = document.getElementById('upsetEmoji');
const upsetPercentEl= document.getElementById('upsetPercent');
const upsetLabelEl  = document.getElementById('upsetMoodLabel');
const upsetSubEl    = document.getElementById('upsetMoodSub');
const upsetRingEl   = document.getElementById('upsetRing');

function updateMoodMeter(val) {
  const mood = MOODS[val];
  upsetEmojiEl.textContent = mood.emoji;
  upsetPercentEl.textContent = val + '%';
  upsetLabelEl.textContent = mood.label;

  upsetSubEl.style.opacity = '0';
  setTimeout(() => {
    upsetSubEl.textContent = mood.sub;
    upsetSubEl.style.opacity = '1';
  }, 160);

  // bounce the emoji
  upsetEmojiEl.style.transform = 'scale(1.35)';
  setTimeout(() => { upsetEmojiEl.style.transform = 'scale(1)'; }, 280);

  // ring border transitions from sage → gold → rose → rose-deep
  if (val <= 20) {
    upsetRingEl.style.borderColor = 'rgba(122, 138, 111, 0.35)';
    upsetPercentEl.style.color    = 'var(--sage-deep)';
  } else if (val <= 40) {
    upsetRingEl.style.borderColor = 'rgba(193, 144, 86, 0.4)';
    upsetPercentEl.style.color    = 'var(--gold)';
  } else if (val <= 70) {
    upsetRingEl.style.borderColor = 'rgba(184, 92, 78, 0.45)';
    upsetPercentEl.style.color    = 'var(--rose)';
  } else {
    upsetRingEl.style.borderColor = 'rgba(146, 64, 47, 0.7)';
    upsetPercentEl.style.color    = 'var(--rose-deep)';
  }

  // 💔 shooting star at 100%
  if (val === 100) fireShootingStar();
}

// ============ SHOOTING STAR — BROKEN HEART FLAIR ============
function fireShootingStar() {
  // don't stack: if one is mid-flight, skip
  if (document.querySelector('.star-overlay')) return;

  const overlay = document.createElement('div');
  overlay.className = 'star-overlay';

  const flair = document.createElement('div');
  flair.className = 'star-flair';

  // wide background halo
  const halo = document.createElement('div');
  halo.className = 'star-glow-halo';
  flair.appendChild(halo);

  // core tail beam
  const tail = document.createElement('div');
  tail.className = 'star-tail';
  flair.appendChild(tail);

  // the broken heart "star"
  const heart = document.createElement('span');
  heart.className = 'star-heart';
  heart.textContent = '💔';
  flair.appendChild(heart);

  overlay.appendChild(flair);
  document.body.appendChild(overlay);

  // spawn ember sparks at intervals along the path
  const totalMs   = 1900;
  const emberCount = 14;
  for (let i = 0; i < emberCount; i++) {
    setTimeout(() => {
      if (!overlay.isConnected) return;
      const progress = i / (emberCount - 1);
      const ease     = progress * progress; // mirrors starFlight easing

      // position on the diagonal (matching the CSS animation)
      const vw = window.innerWidth;
      const vh = window.innerHeight;
      const travelX = vw + 180;
      const travelY = vh + 180;
      const x = -90 + -travelX * ease;   // rightward → leftward
      const y = -90 + travelY  * ease;

      // convert to viewport coords
      const screenX = vw - (-x);   // right: -90 → right-anchored
      const screenY = y;

      const size = Math.random() * 7 + 3;
      const em   = document.createElement('div');
      em.className = 'star-ember';
      const angle = Math.random() * Math.PI * 2;
      const dist  = Math.random() * 36 + 12;
      em.style.cssText = `
        width: ${size}px;
        height: ${size}px;
        right: ${Math.max(0, (vw - screenX) - size / 2)}px;
        top: ${Math.max(0, screenY - size / 2)}px;
        background: rgba(${200 + Math.random() * 55|0}, ${80 + Math.random() * 60|0}, ${60 + Math.random() * 40|0}, 0.95);
        --dx: ${Math.cos(angle) * dist}px;
        --dy: ${Math.sin(angle) * dist - 20}px;
        --dur: ${0.5 + Math.random() * 0.4}s;
        box-shadow: 0 0 ${size * 2}px rgba(220, 90, 70, 0.7);
      `;
      overlay.appendChild(em);
    }, i * (totalMs * 0.85 / emberCount));
  }

  // remove overlay after animation finishes
  setTimeout(() => overlay.remove(), totalMs + 400);
}

upsetSlider.addEventListener('input', (e) => {
  updateMoodMeter(parseInt(e.target.value));
});

// initialise at default 10%
updateMoodMeter(10);

// SCROLL FADE-IN
const fadeObserver = new IntersectionObserver((entries) => {
  entries.forEach(e => {
    if (e.isIntersecting) {
      e.target.classList.add('visible');
      fadeObserver.unobserve(e.target);
    }
  });
}, { threshold: 0.12 });
$$('.fade-section').forEach(s => fadeObserver.observe(s));

const vowObserver = new IntersectionObserver((entries) => {
  entries.forEach(e => {
    if (e.isIntersecting) {
      e.target.classList.add('visible');
      vowObserver.unobserve(e.target);
    }
  });
}, { threshold: 0.4 });
$$('.vow-item, .vow-divider, .vow-closing').forEach(v => vowObserver.observe(v));
</script>
</body>
</html>
